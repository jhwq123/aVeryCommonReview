mixin post(post)
    a(href=`/posts/${post.id}`).post-mixin
    div.post-mixin__thumb(style=`background-image:url(${post.thumbUrl});background-size:cover;background-position:center;`)
    div.post-mixin__data
        span.post-mixin__title=post.title 
        div.post-mixin__meta
            span #{post.owner.name} •
            span #{post.meta.views}   views